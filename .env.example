REACT_APP_BLOCKFROST_ID=your_preprod_blockfrost_project_id
REACT_APP_CHARLI3_KEY=your_charli3_api_key

# Optional: tweak guard thresholds via code if you fork,
# but keep all secrets in .env and never commit a real .env file.

